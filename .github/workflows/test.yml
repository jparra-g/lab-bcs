name: Platform CI

on:
  push:
    tags:
    - "*"

permissions:
  id-token: write
  contents: read    # This is required for actions/checkout

jobs:
  deploy:
    name: Deploy

    steps:
    
      run: |
          echo "ok push tag"